Napisz skrytp w PHP który:
- losuje 10 dowolnych liczb dwucyfrowych, ładuje je do tablicy i wyświetla
- sortuje te liczby metodą bąbelkową i wyświetla na nowej stronie (header("Location: s2.php")
- tworzy formularz do wpisania przez usera tylu liczb ile on chce i po wpisaniu wyświetli je w koleności usera jak i posortowane

$k = rand(10,100)
for ($i=1 ; $i < $n ; $i++)
{

}

<ipnut name="x1">
$_POST["x1"]